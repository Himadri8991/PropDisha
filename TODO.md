# TODO

- [ ] Fix axe-linter issue in `src/components/sections/FeaturedProjects.tsx` where a `<button>` has no discernible text.
- [ ] Add accessible text (or `aria-label`) to the empty buttons (likely the scroll progress dots).
- [ ] Re-run lint to confirm no axe violations remain.

